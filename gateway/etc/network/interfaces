# WAN: upstream router
auto eth0
iface eth0 inet static
    address ${DEVICE_IP}
    netmask 255.255.255.0
    gateway ${GATEWAY}

# LAN: static IP for access point clients
auto wlan0
iface wlan0 inet static
    address 192.168.2.1
    netmask 255.255.255.0
