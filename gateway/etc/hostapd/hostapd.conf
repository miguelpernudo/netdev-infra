# hostapd turns the Wi-Fi card into a network itself.

ssid=Red_Miguel
wpa_passphrase=${WPA_PASSPHRASE}

interface=wlan0
driver=nl80211
ieee80211n=1

hw_mode=g
channel=6
wmm_enabled=1

auth_algs=1
wpa=2
wpa_key_mgmt=WPA-PSK
rsn_pairwise=CCMP
ap_isolate=1
